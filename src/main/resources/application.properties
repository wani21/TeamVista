# Application Configuration
spring.application.name=team-productivity-backend
server.port=9090

# H2 Database Configuration (Default)
spring.datasource.url=jdbc:h2:mem:productivitydb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.defer-datasource-initialization=true

# H2 Console (for development)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JWT Configuration
jwt.secret=mySecretKeyForTeamProductivityDashboardApplication2025
jwt.expiration=86400000

# MySQL Configuration (Alternative - uncomment to use)
# spring.datasource.url=jdbc:mysql://localhost:3306/productivitydb
# spring.datasource.username=root
# spring.datasource.password=Engineering!123
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Logging Configuration
logging.level.com.productivity.dashboard=DEBUG
logging.level.org.springframework.security=DEBUG
